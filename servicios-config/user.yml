server:
  servlet:
    context-path: /api/v1/poli
  port: 9088
spring:
  application:
    name: user
  profiles:
    active: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka/